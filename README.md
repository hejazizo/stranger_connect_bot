# Stranger Connnect Telegram Bot

## How to Run
First add `PYTHONPATH` environment variable in repository root directory by running:
```
export PYTHONPATH=${PWD}
```

Then run:
```bash
python src/main.py
```